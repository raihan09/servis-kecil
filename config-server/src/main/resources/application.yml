spring:
  application:
    name: config-server
  cloud:
    config:
      server:
         git:
          uri: https://github.com/raihan09/servis-kecil.git
          searchPaths: servis-kecils-config
          default-label: main
          clone-on-start: true
server:
  port: 8888
management:
  endpoints:
    web.exposure.include: "*"
